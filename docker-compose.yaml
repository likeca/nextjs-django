services:
  backend:
    image: likeca/django
    container_name: backend
    build:
      context: .
    ports:
      - "8000:8000"
    env_file:
      - backend/nextjs_django/settings/local.env

  frontend:
    image: likeca/nextjs
    container_name: frontend
    build:
      context: .
    ports:
      - "3000:3000"
    env_file:
      - frontend/.env
